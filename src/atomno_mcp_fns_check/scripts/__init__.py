"""CLI-утилиты atomno-mcp-fns-check (запускаются через `python -m`).

Содержит вспомогательные скрипты, не относящиеся к рантайму MCP-сервера:

  * `etl_registries`  — ETL Open Data ФНС → локальный SQLite реестров
                        (массовые адреса / руководители / дисквалифицированные).
"""
